This folder contains the lab source Markdown and images.

To convert the lab to DOCX (requires `python-docx`):

```powershell
python -m pip install python-docx
python md2docx.py lab.md lab.docx
```

The produced file will be `lab.docx` in this folder.
